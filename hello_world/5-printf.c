#include <stdio.h>
/**
 * Same as last one but using PRINTF this time cause it be better
 *
 *
 */
int main(void)
{
    printf("with proper grammar, but the outcome is a piece of art,\n");
    return (0);
}